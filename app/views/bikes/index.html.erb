<h1>Bikes around you</h1>
<div class=row>
  <div class=col-6>
    <div class="container">
      <h3>Find a bike around you</h3>
      <div class="cards-grid">
        <%= render 'bikes/cards-grid', bikes: @bikes %>
      </div>
    </div>
  </div>

  <div class=col-6>
      <div style="width: 100%; height: 600px;"
      data-controller="map"
      data-map-markers-value="<%= @markers.to_json %>"
      data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
    </ul>
  </div>
</div>
